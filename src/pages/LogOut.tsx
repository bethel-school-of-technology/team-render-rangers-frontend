import React from 'react';

const LogOut = () => {
  const handleLogout = () => {

};

  return (
    <div>
      <button onClick={handleLogout}>Log Out</button>
    </div>
  );
};

export default LogOut;